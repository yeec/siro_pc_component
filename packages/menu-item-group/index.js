import ComMenuItemGroup from '../menu/src/menu-item-group';

/* istanbul ignore next */
ComMenuItemGroup.install = function(Vue) {
  Vue.component(ComMenuItemGroup.name, ComMenuItemGroup);
};

export default ComMenuItemGroup;
