import ComButtonGroup from '../button/src/button-group';

/* istanbul ignore next */
ComButtonGroup.install = function(Vue) {
  Vue.component(ComButtonGroup.name, ComButtonGroup);
};

export default ComButtonGroup;
